1. bigwig -> (bigwig2bedgraph) -> bedgraph -> (bedgraph2bed, add strand info and peak ID) -> bed -> MHCan -> LILY -> ( Super enhancer ) -> homer peak annotation / motif enrichment
2. broad bed -> homer peak annotation / motif enrichment
